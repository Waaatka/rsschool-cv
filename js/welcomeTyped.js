import Typed from "typed.js";

const options = {
  strings: ["Hi, I'm Anna Sapegina", "I'm a front-end developer"],
  typeSpeed: 100,
  startDelay: 1000,
  backDelay: 1000,
  loop: true,
};
const typed = new Typed(".typed span", options);
